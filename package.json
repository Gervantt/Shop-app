{
  "name": "shop-app",
  "version": "1.0.0",
  "description": "Shop App — React + Express + Prisma + PostgreSQL",
  "scripts": {
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
